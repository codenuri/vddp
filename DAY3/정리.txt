디자인 패턴 
=> 특정 문제를 해결하기 위해 사용된 "전형적인 코딩 스타일"에 "이름"을 부여 한것
=> GOF's 책에서는 23개의 이름을 소개
   (template method, strategy, prototype, ... )

=> 대부분의 객체지향 언어(C++, C#, Java등)로 구현 가능한 일반적인 기법 사용 


IDioms
=> 각 언어의 문법적 특징을 고려한 디자인 기법
=> C++ IDioms, Java IDioms, C# IDioms
=> 구글 "C++ IDioms" 에서 1번째 링크

=> 100 여개의 C++ IDioms 존재 
   ( Policy Base Design, ... )

이런 내용을 제대로 학습 하려면
1. C++ 기본 문법 학습
2. 디자인 패턴 23개를 학습 + C++ 문법을 좀더 학습
3. C++ IDioms 내용 학습
-----------------------------



==============================================================================
디자인 패턴의 핵심 원리 #1. 변하지 않은 코드에서 변하는 코드는 분리하는 것

#1. 변하는 코드는 가상함수로 분리
=> 의도 : 파생 클래스를 만들어서 가상함수를 override 하라는 것
=> 예제 : 도형편집기 예제의 draw()/draw_imp()
=> template method


#2. 변하는 코드를 다른 클래스로 분리(정책 클래스)

정책 클래스가 지켜야 하는 규칙을 어떻게 약속하고, 
정책 클래스를 어떻게 교체 가능하게 할것 인가 ?

strategy           : 함수 이름을 인터페이스로 약속하고, 인터페이스를 사용해서 교체 가능하게!
policy base design : 함수 이름을 문서화로 약속하고, template 인자를 통해서 교체 가능하게!


						성능				          실행시간 교체 가능 여부
strategy 			: 가상함수기반, 약간의 성능 저하,    실행시간 교체 가능
													   edit.set_validator(&v1)
													   edit.set_validator(&v2)
policy base design 	: 인라인치환가능. 빠르다.			실행시간 교체 안됨
														vector<int, debug_alloc<int>> v;

=> vector 의 메모리 할당 정책을 실행시간에 변경할 이유는 없다!
=> 따라서, 이 경우는 policy base design 이 더 좋다.
-------------------------------------------------------------
디자인 패턴의 핵심 #2. 다양한 이유로 "공통의 기반 클래스"를 만드는 경우가 많습니다

#1. Folder 는 File 을 보관하지만 Folder 자체도 보관할수 있다.
    => Folder 와 File 은 공통의 기반 클래스 필요

#2. FileStream 에 ZipDecorator 로 압축기능을 추가한 상태로, 다시 암호화 기능 추가가 가능
    => 기능의 중첩 추가가 가능
	=> FileStream 과 기능추가 클래스(Decorator) 는 공통의 기반 클래스가 필요 하다

composite : 공통의 기반 클래스를 사용해서 "복합객체"를 만들때 사용하는 패턴 - 객체의 포함관계
decorator : 공통의 기반 클래스를 사용해서 객체에 "기능"을 동적으로 중첩해서 추가하는 패턴

// 위 2개 패턴의 class diagram 은 유사합니다
// => 헷갈릴때는 항상 "의도" 를 파악하세요

// 디자인 패턴을 열심히 공부하면
// => 다양한 패턴이 서로 유사해 보이는 경우가 있습니다.
// => 이럴때는 각 패턴의 "의도" 를 생각해라!!

//--------------------------------------------------------------
// 디자인 패턴의 핵심 원리 #3. 객체를 생성하는 다양한 방법을 알아 두세요

class Rect
{
};

// 객체를 만드는 방법

// 1. Rect rc;	// 지역변수
				// 수명이 정해져 있다 {}을 벗어날때 파괴
				// => 개발자가 원하는 시점에 파괴 할수는 없다.


// 2. Rect* rc = new Rect;	// 가장 자유로운 방법
//							// 원하는 시점에 제거 가능(원할때 delete )
							// 그런데, 자유롭다는 것이 가장 좋다는 의미는 아님!!


// 초급 시절에는 위 2가지 방법만 생각하지만...
// 아래 3가지 방법도 고려해 보세요..


// 3. Rect* rc = Rect::create()  :	static 멤버 함수 사용
//									=> 객체의 생성을 한곳(create)에서만 생성하므로
//									   다양한 제약을 사용할수 있다.
//									A. 오직 한개만 만들수 있게 -> 싱글톤.  get_instance()
//									B. 속성이 동일하면 공유    -> 플라이 웨이트 Image::create()
//									C. 생성함수주소를 함수 인자로 전달하거나
//										자료구조에 보관 - Factory 예제 
// static 멤버 함수를 사용해서 객체를 생성하는 기술은 너무나 유명합니다.
// => 자바 진영에서는 이기술을 "static factory method" 라는 용어로 사용합니다.


// 4. Rect* rc = prototype->clone();  : 복사본을 사용한 객체 생성 - Prototype

// 5. Rect* rc = factory.create() : 공장을 통한 객체 생성
//-----------------------------------------------------------------

// 객체지향 디자인 패턴의 핵심 #4. 다양한 문제를 해결하기위해 중간 계층(간접층)을 도입하는것
// 해결하기 어려운 문제가 있으면 
// => 중간 계층을 생각하세요.(level of indirection)
// => 다양한 해결책이 생기게 됩니다.

// 사용자 =================== std::stack ====================> std::list
//						 중간 계층의 의도 : 인터페이스의 변경				
//						 adapter 패턴


// 사용자 =================== ZipDecorator ==================> FileStream
						중간 계층의 의도 : 기능의 추가
						decorator 패턴


// 사용자 =================== Calc        ===================> 원격지 서버
						중간 계층의 의도 : 기존 요소의 대행자
						proxy 패턴


// 사용자 =================== BrightnessCommand  ============> m.set_brightness(90)
						중간 계층의 의도 : 객체의 이전상태보관하고있다고, undo 등의 기능 수행
						command 패턴

//---------------------------------------------------------------------
// GOF's 디자인 패턴은 용도에 따라 3가지로 분류 합니다.
// (명확히 분류되지 않고, 외울 필요 없습니다. 중요하지는 않음)


// 생성 패턴 5개 : 객체의 생성에 대한 코딩 기술
// => singleton        : 오직 한개만 생성되게 하자 
// => abstract factory : 공장을 통해서 만들자(공장도 교체 할수 있게)
// => prototype        : 자주 사용하는 객체는 견본을 먼저 만들고, 견본을 복사해서 새로운 객체도 만들자
// => factory method   : 객체를 만들기 위한 인터페이스를 정의하고 사용하지만, 
//						 어떤 객체인지는 파생 클래스가 결정.
//						 template method 와 유사한 코드 
// => builder : 생성과정과 표현과정을 분리해서 복잡한 객체를 생성 - C++에서는 잘 사용안함. 
//																java 에서 많이 사용


// 구조 패턴 7개 : 구조의 변경
// composite : 복합객체를 tree 구조로 구축
// decorator : composite 와 유사한 클래스 다이어 그램이지만, 객체의 포함이 아닌 기능의 중첩 추가
// adapter   : 인터페이스의 변경
// proxy     : 대행자 만들기
// flyweight : 속성이 동일하면 공유하자
// bridge    : 구현과 추상의 분리를 통한 독립적 update <============== 중요
// facade    : 복잡함을 단순화하는 클래스 제공    

// 행위 패턴 11개 : 행동의 변경
// template method : 변하는 것을 가상함수로
// strategy        : 변하는 것을 다른 클래스로, 알고리즘의 교체
// observer        : 1 : N 의 구조, 1 이 변경되면 N 에게 통보
// command         : 명령의 캡슐화를 통한 undo/redo 의 구현

// iterator : STL 반복자 이야기
// visitor  : 복합객체의 모든 요소를 방문해서 연산
// chain of responsibility : 책임을 전달
// state    : 동작의 교체  <==========================  학습해 보세요
// memento  : 객체를 저장/복구
// interpreter : 컴파일러 파서 만들때 사용, 지금은 거의 사용안함

// mediate : 중재자 패턴, 객체간 관계가 복잡하면 중재자를 만들어라

//-----------------------------
// 디자인 패턴 : 전형적인 코딩 형태에 대한 이름(대부분의 객체지향 언어에서 구현 가능)
// IDioms     : 언어의 특징을 고려한 디자인 
//              => policy base design, CRTP, RAII,

// SOLID : 프로그램을 만들때 "이것만은 지켜라" 라는 5개의 원칙
// => 학술 논문으로 발표된것

// SOLID 
// SRP : Single Responsibility Principle => 하나의 클래스는 하나의 책임(기능)만!!
// OCP : Open Close Principle            => 기능확장에 열려 있고, 코드 수정에는 닫혀 있어야!!
// LSP : Liskov Substitution Principle
// ISP : Interface Segregation Principle
// DIP : Dependency Inversion Principle        


//-------------------------------------------------------
// LSP ( Liskov Substitution Principle )
// => 리스코프 라는 사람이 만든 치환의 법칙
// => 기반 클래스가 필요한 곳은 파생 클래스로 치환할수 있어야 한다.

class Car
{
	// 다양한 멤버 들.. 
public:
	virtual void Go() { std::cout << "Go\n"; }
};

class SuperCar : public Car
{
public:
	void Go() override { std::cout << "fast Go\n"; }
};

class FlyingCar : public Car
{
public:
	void Fly() {}
	void Go() override { throw UnsupportedOperation(); }
};

void foo(Car* c) // 기반 클래스가 사용되는 곳
{
	c->Go();
}

foo(new SuperCar);	// LSP 위반 아님.
foo(new FlyingCar); // LSP 위반!!! 기반 클래스 기능이 제거 되었다
					// 나쁜 코드!!

//---------------------------------------------------
// ISP ( Interface Segregation Principle )
// => 인터페이스의 격리(분리)의 원칙
// => 각 클라이언트가 필요로 하는 인터페이스들을 분리함으로써, 
//    클라이언트가 사용하지 않는 인터페이스에 변경이 발생하더라도 
//    영향을 받지 않도록 만들어야 하는 것이 핵심이다.

// 아래 코드는 나쁜 인터페이스
/*
struct IMP3
{
	virtual void play() = 0;
	virtual void stop() = 0;
	virtual void upload_sns() = 0;
	virtual ~IMP3() {}
};
*/

// 아래 코드가 좋은 인터페이스
struct IMP3
{
	virtual void play() = 0;
	virtual void stop() = 0;
	virtual ~IMP3() {}
};

struct ISNS
{
	virtual void upload_sns() = 0;
	virtual ~ISNS() {}
};

void only_listening_music(IMP3* mp3)
{
	mp3->play();
	mp3->stop();
}

//----------------------------------------------
// DIP ( Dependency Inversion Principle )
// => 의존 관계 역전의 법칙
// => 상위 모듈은 하위 모듈의 세부 구현에 의존하면 안된다.
// => 상위 모듈은 추상에 의존해야 한다.
// => 세부 사항에 의존하지 말고, 추상에 의존하라는 것
class People
{
public:
	void use(HDCamera* c) { c->take(); }  // DIP 위반
	void use(ICamera* c) { c->take(); }  // DIP 준수

	// People 은 카메라가 필요하지만, 
	// 1. 구체적인 하위 모듈(HDCamera)에 의존하지 않고
	// 2. 추상(ICamera) 에 의존하게 된다.
};




